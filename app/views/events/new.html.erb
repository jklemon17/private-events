<%= form_for(@event) do |f| %>
  <%= f.label(:name) %>
  <%= f.text_field(:name) %><br>
  <%= f.label(:date) %>
  <%= f.date_field(:date) %><br>
  <%= f.label(:location) %>
  <%= f.text_field(:location) %><br>

  <%= f.submit('Create!') %>
<% end %>
